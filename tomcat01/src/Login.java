public class Login{
    public boolean isLogin(String name, String passwd){
        if("hujun".equals(name) && "hujun".equals(passwd)){
            return true;
        }else{
            return false;
        }
    }
}